$('.slider-principal').slick({
    // IMPORTANT!
    // With those instructions, ever the last command is without commom
    dots: true,
    infinite: true,
    speed: 300,
    slidesToShow: 1,
    adaptiveHeight: true,
    autoplay: true,
    autoplaySpeed: 2000
  });